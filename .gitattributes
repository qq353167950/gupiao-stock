# 行尾规范化：仓库内统一 LF，保证 Linux 容器内脚本可直接执行
* text=auto eol=lf

# Windows 专用脚本保留 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制文件不做转换
*.png binary
*.jpg binary
*.ico binary
*.db binary
*.woff binary
*.woff2 binary
